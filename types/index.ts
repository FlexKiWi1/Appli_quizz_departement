
export type Quiz = {
    id: number;
    name: string;
    level: number;
}

export type Department = {
    nom?: string;
    code?: string;
    codeRegion?: string;
}

export type Region = {
    nom?: string;
    code?: string;
}